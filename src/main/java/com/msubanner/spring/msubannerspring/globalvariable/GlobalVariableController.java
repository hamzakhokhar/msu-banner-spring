package com.msubanner.spring.msubannerspring.globalvariable;


import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.servlet.ModelAndView;

import javax.validation.Valid;

/**
 * This is the ProfessorController class.
 * @author Team 3
 */

@Controller
public class GlobalVariableController {/*

        @Autowired
        private GlobalVariableRepository globalvariableRepository;


        @RequestMapping(value = "/globalvariable", method = RequestMethod.GET)
        public ModelAndView getProfessorView() {
            return new ModelAndView("globalvariable/index", "globalvariable",
                                    globalvariableRepository.findAll());
        }

    */}